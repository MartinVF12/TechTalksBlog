// Archivo de entrada para los modelos
const User = require('./User');
const Post = require('./Post');
const Comment = require('./Comment');

// Definir relaciones entre modelos si es necesario

module.exports = { User, Post, Comment };
